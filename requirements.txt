# ============================================================
# Web UI
# ============================================================
gradio>=4.44.0,<5.0

# ============================================================
# ML frameworks
# ============================================================
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0

# ============================================================
# Speech pipeline
# ============================================================
faster-whisper>=1.0.0
pyannote.audio>=3.1.0,<4.0
funasr>=1.0.0,<2.0
speechbrain>=0.5.16

# ============================================================
# Annotation & audio utilities
# ============================================================
pympi-ling>=1.70
jieba>=0.42.1
librosa>=0.10.0
soundfile>=0.12.0

# NOTE: ffmpeg is required at the system level (audio extraction runs it as a
# subprocess).  On Hugging Face Spaces it is installed via packages.txt;
# in the Dockerfile it is installed via apt.
