# Auto Transcriber & Annotation Tool — 用户手册

**版本**: v1.0　|　**适用平台**: macOS / Linux / Windows　|　**语言**: 中文

---

## 1. 引言：为什么要用这个工具？

### 传统 ELAN 转写的痛点

如果你使用过 ELAN 进行视频/音频转写，你很可能经历过以下困扰：

- **逐句听写耗时长**：一段 10 分钟的视频，纯手工转写可能需要 1–2 小时，如果包含多个说话人则更久。
- **说话人分配繁琐**：需要反复回听确认"这句话是谁说的"，然后在对应的 Tier 上手动创建标注。
- **时间轴调整费力**：ELAN 中微调标注边界需要放大波形、精细拖拽，新手很容易误操作。
- **标点和分词靠人工**：ELAN 不会自动添加标点或分词，需要手动处理。
- **层结构维护困难**：多人协作时，Tier 命名不统一、层级关系混乱是常态。

### 本工具的解决方案

`transcript` 将上述流程中可自动化的部分交给 AI，你只需要在自动生成的结果上做**校对和微调**：

| 痛点 | 本工具对应方案 |
|------|---------------|
| 手动听写耗时 | Whisper 自动语音识别，生成初始转写文本 |
| 说话人分配繁琐 | pyannote 自动说话人分离，映射阶段一键分配到层 |
| 时间轴调整费力 | 可视化拖拽编辑器，拖拽边缘或整体移动 |
| 标点/分词靠人工 | FunASR 自动标点恢复 + jieba 中文分词 |
| 层结构混乱 | 模板 EAF 预定义层级，自动解析 PARENT_REF |

---

## 2. 工作流对比：传统 ELAN vs 本工具

以下对比基于一段 **10 分钟、3 个说话人** 的中文视频的实际测试经验。

| 步骤 | 传统 ELAN 转写 | 本工具 (transcript) | 本工具节省时间 |
|------|---------------|---------------------|:---:|
| **1. 创建项目与层** | 打开 ELAN → 新建 EAF → 逐一手动创建每个 Tier → 设置 Linguistic Type → 配 PARENT_REF | 上传模板 EAF（如有）或自动生成层，一键完成 | **约 10 倍** |
| **2. 导入媒体与对齐** | 手动导入视频和音频文件，检查对齐 | 上传 MP4，自动提取音频并对齐 | **即时** |
| **3. 转写文本** | 逐句听音频，手动输入文本（10 分钟约需 30–60 分钟） | Whisper 自动识别，约 1–3 分钟完成（视模型大小） | **约 10–20 倍** |
| **4. 说话人分配** | 人工判断每句话的说话人，手动分配到对应 Tier | 自动分离说话人，逐一听样本后下拉选择映射（约 2–5 分钟） | **约 5 倍** |
| **5. 时间轴调整** | 逐句放大波形，拖拽起止边界（最容易出错） | 可视化色块拖拽，拖边缘调时间，拖中间整体移动 | **约 5 倍** |
| **6. 标点与分词** | 手动添加标点，手动分词 | 自动标点恢复 + jieba 分词，附带时间戳 | **约 10 倍** |
| **7. 导出** | File → Save As → 选择路径 | 一键导出 EAF + PFSX 到指定目录 | **相同** |

> **综合估算**：一段 10 分钟视频从零到完整标注，传统 ELAN 约需 **1.5–3 小时**，使用本工具约需 **15–30 分钟**（包含人工校对），效率提升约 **5–10 倍**。

---

## 3. 功能详解

### 3.1 视频与模板上传（Step 1）

**支持的视频格式**: MP4

**模板 EAF（可选）**: 上传已有 EAF 文件作为模板，系统会自动解析：
- Linguistic Type 定义
- Tier 列表和顺序
- PARTICIPANT 关联
- **PARENT_REF 层级关系**（如 `interaction@INB` → 父层 `INB`，或 `@` 命名约定自动识别）

> **提示**：如果你的研究项目有固定的编码方案（如 CHAT、CHILDES），建议先做好一个模板 EAF，每次处理新视频时上传即可。

**其他设置**：
- 说话人数量（可选）：留空则自动检测；如果已知确切人数，可指定以获得更准确的分离结果
- 输出目录：默认 `~/Desktop`，可自定义

点击 **"开始处理"**，进度条会实时显示处理阶段。

---

### 3.2 自动转写流水线（Step 1 → Step 2）

处理过程依次运行以下 7 个阶段：

```
MP4 上传
  │
  ├─ 1. 音频提取 ── ffmpeg → 16kHz 单声道 WAV
  ├─ 2. 语音识别 ── faster-whisper → 带时间戳的文本
  ├─ 3. 说话人分离 ── pyannote.audio 3.1 → 每个说话人的时间区间
  ├─ 4. 时间对齐 ── ASR 结果 + 说话人结果 → 精确匹配
  ├─ 5. 标点恢复 ── FunASR ct-punc → 添加标点符号
  ├─ 6. 句子拆分 ── 按标点拆分长段
  └─ 7. 音频片段 ── 为每个说话人提取代表性音频
```

处理完成后进入 Step 2，可查看转写结果摘要（说话人数、段数）。

---

### 3.3 说话人映射（Step 3）

这是自动转写流程中**唯一需要人工判断**的步骤。

每个检测到的说话人会显示一张卡片，包含：

| 卡片元素 | 说明 |
|---------|------|
| 🎧 音频播放器 | 播放该说话人的代表性语音片段，帮助辨识身份 |
| 📖 转写样本 | 该说话人最长的三句话，供参考语境 |
| 🏷️ 层名选择 | 下拉框，选项来自模板 EAF 的 Tier 列表 |

**操作流程**：

1. 点击播放按钮，听该说话人的声音
2. 查看转写样本，辅助判断
3. 在下拉框中选择对应的层名（如 `Teacher`、`Child`），或直接输入自定义名称
   - 选择 `xxx` 表示该说话人为儿童（部分研究项目的约定）
   - 留空则保留默认 ID（如 `SPEAKER_00`）
4. 对所有说话人完成映射后，点击 **"进入时间轴编辑"**

---

### 3.4 时间轴编辑器（Step 4）— 核心优势

这是本工具最大亮点，也是与传统 ELAN 工作流差异最大的部分。

#### 界面布局

```
┌──────────────────────────────────────────┐
│  ┌─────────────┐  ┌──────────────────┐  │
│  │  视频播放器  │  │  控制面板        │  │
│  │             │  │  • 层复选框      │  │
│  │ ▶ ⏸ 进度条  │  │  • 搜索框       │  │
│  │             │  │  • 编辑面板      │  │
│  └─────────────┘  │  • 撤销/重做     │  │
│                   └──────────────────┘  │
├──────────────────────────────────────────┤
│  层名  │  时间轴网格（可滚动）            │
│ ──────┼────────────────────────────────  │
│  老师  │  [███████]  [████]  [██████]    │
│  小明  │     [█████████]     [█████]     │
│  xxx   │  [███]                    [███] │
│        │  ▲ 红色播放头                    │
└──────────────────────────────────────────┘
```

#### 操作速查表

| 操作 | 方式 | 对应 ELAN 操作 |
|------|------|---------------|
| **选择 segment** | 点击色块 | 点击标注行 |
| **修改文本** | 右侧面板编辑，实时生效 | 双击标注 → 输入文本 |
| **调整起止时间** | 拖拽色块左/右边缘 | 拖拽标注边界 |
| **整体移动** | 拖拽色块中间 | 按住标注拖拽 |
| **变更层归属** | 拖拽色块到另一个层的行 | 剪切标注 → 粘贴到另一个 Tier |
| **新建 segment** | 双击时间轴空白区域 | 选中 Tier → 创建标注 |
| **删除 segment** | 选中后点击 Delete 按钮 | 右键 → Delete Annotation |
| **撤销** | Ctrl+Z 或点击 ↩ 按钮 | Edit → Undo |
| **重做** | Ctrl+Shift+Z 或点击 ↪ 按钮 | Edit → Redo |
| **搜索** | 在搜索框输入关键字，匹配的色块高亮 | 无内置等效功能 |
| **隐藏/显示层** | 取消/勾选层复选框 | 无内置等效功能 |
| **展开/折叠子层** | 点击层名旁的 ▶/▼ | 无内置等效功能 |
| **缩放时间轴** | Ctrl+滚轮 | 界面底部缩放滑块 |
| **播放视频** | 点击视频播放按钮 | 点击播放按钮 |

#### 层级显示

继承自模板 EAF 的 **PARENT_REF** 关系会自动以缩进方式显示：

```
  INB               ▼  ← 父层，可展开/折叠
    interaction@INB    ← 子层（缩进显示）
    attention@INB
  CHI               ▼
    speech@CHI
    vocal@CHI
```

折叠父层时，所有子层的时间轴行也会同步隐藏。

---

### 3.5 导出 EAF（Step 5）

在 Step 4 中完成所有编辑后，点击 **"📦 应用修改并生成 EAF"**。

**输出文件**：
- `output.eaf` — ELAN 标注文件，可在 ELAN 中直接打开继续编辑
- `output.pfsx` — Phon Session XML 配套文件

**导出特性**：
- 保留模板 EAF 的层级结构和属性
- 所有手动修改（文本、时间、层归属、新增/删除）都会反映在导出文件中
- 文件生成到 Step 1 中指定的输出目录

---

## 4. 安装与启动

### 4.1 系统要求

| 组件 | 要求 |
|------|------|
| 操作系统 | macOS（推荐）/ Linux / Windows |
| Python | 3.9 或以上 |
| FFmpeg | 必须安装 |
| 磁盘空间 | 10–20 GB（模型首次自动下载） |
| 内存 | 建议 8 GB+ |

### 4.2 安装步骤

```bash
# 1. 进入项目目录
cd /path/to/transcript

# 2. 创建虚拟环境（推荐）
python3 -m venv venv
source venv/bin/activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 安装 FFmpeg（如未安装）
brew install ffmpeg          # macOS
sudo apt install ffmpeg       # Ubuntu/Debian

# 5. 配置 Hugging Face Token
echo 'HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx' > .env
```

### 4.3 获取 Hugging Face Token

1. 注册 [huggingface.co](https://huggingface.co) 账号
2. 在 [Settings → Access Tokens](https://huggingface.co/settings/tokens) 创建 token
3. 接受以下模型许可：
   - [pyannote/speaker-diarization-3.1](https://huggingface.co/pyannote/speaker-diarization-3.1)
   - [pyannote/segmentation-3.0](https://huggingface.co/pyannote/segmentation-3.0)
4. 将 token 写入 `.env` 文件

### 4.4 启动

```bash
python3 app.py
```

浏览器打开 **http://127.0.0.1:7860**。

macOS 用户也可双击桌面的 `启动转录工具.command` 一键启动。

---

## 5. 常见问题

### Q1: 端口 7860 被占用怎么办？

```bash
lsof -ti:7860 | xargs kill -9
python3 app.py
```

### Q2: `ffmpeg: command not found`

FFmpeg 未安装或未加入系统 PATH。运行 `ffmpeg -version` 验证。

### Q3: `HF_TOKEN is required`

- 确认 `.env` 文件在项目根目录
- 确认已接受 pyannote 模型许可
- 确认 token 以 `hf_` 开头

### Q4: 模板 EAF 解析失败

常见原因：
- EAF 文件损坏或格式不正确（用 ELAN 打开验证）
- Tier 使用了不被支持的 Linguistic Type
- 检查终端输出的具体错误信息

### Q5: 导出后修改没有生效

- 确保点击的是 Step 4 的 **"📦 应用修改并生成 EAF"** 按钮
- 建议每次做重要修改后留意浏览器控制台（F12 → Console）是否有 `[syncToParent]` 成功日志
- 如果导出的仍是原始数据，刷新页面重试

### Q6: 如何升级 Whisper 模型？

```bash
export WHISPER_MODEL="medium"
python3 app.py
```

或修改 `config.py` 中的 `WHISPER_MODEL` 变量。

**模型选择建议**：

| 场景 | 推荐模型 |
|------|---------|
| 日常中文对话（安静环境） | `small`（默认） |
| 嘈杂环境 / 多人重叠 | `medium` |
| 快速预览 / 低配机器 | `base` |
| 最高准确率要求 | `large` |

### Q7: Layer 太多，时间轴编辑卡顿？

- 使用层复选框隐藏不需要编辑的层
- 折叠不操作的父层
- 使用搜索功能快速定位目标 segment

---

## 6. 对比总结

> **传统 ELAN 转写**：全手动操作，适合对控制精度有极高要求的场景，但耗时巨大。标注人员需要同时处理"听、判断、输入、对齐"四项任务，认知负荷高。
>
> **本工具**：将"听写"和"说话人判断"交给 AI 完成初稿，标注人员只需**校对和微调**。时间轴调整从"放大波形逐帧拖拽"变成"肉眼可见的色块拖拽"。输出文件**与 ELAN 完全兼容**，可无缝融入现有工作流。
>
> 简而言之：**AI 负责 80% 的体力活，你负责 20% 的精细校对**。

| 维度 | 传统 ELAN | 本工具 |
|------|----------|--------|
| 自动化程度 | 0%，纯手动 | ~80%，自动生成初稿 |
| 10 分钟视频耗时 | 1.5–3 小时 | 15–30 分钟 |
| 操作门槛 | 需学习 ELAN 界面和概念 | 网页操作，直观拖拽 |
| 说话人识别 | 完全依赖人工判断 | AI 分离 + 人工确认 |
| 层级管理 | 手动创建和维护 | 模板复用，自动解析 |
| 协作一致性 | 依赖人工约定 | 模板保证层结构统一 |
| 输出兼容性 | EAF（原生） | EAF + PFSX（与 ELAN 完全兼容） |

---

## 7. 致谢

本项目基于以下开源项目构建：

| 项目 | 用途 |
|------|------|
| [faster-whisper](https://github.com/SYSTRAN/faster-whisper) | 语音识别引擎 |
| [pyannote.audio](https://github.com/pyannote/pyannote-audio) | 说话人分离 |
| [FunASR](https://github.com/modelscope/FunASR) | 标点恢复模型 |
| [Gradio](https://github.com/gradio-app/gradio) | Web 界面框架 |
| [pympi-ling](https://github.com/dopefishh/pympi) | EAF/PFSX 读写库 |
| [jieba](https://github.com/fxsjy/jieba) | 中文分词 |
| [librosa](https://github.com/librosa/librosa) | 音频处理 |

---

> 📧 如有问题或建议，欢迎通过 GitHub Issues 反馈。
